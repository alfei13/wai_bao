App端数据获取：美团快驴、大润发App、食行生鲜、蔡亿萝：
主要的目的：通过获取这些市场端APP的SKU信息。将这些数据保存到数据库，用于业主的场景1供应商报价谈判，场景2业主向客户报价的依据。数据开发的需求仅仅是将这些数据采集并保存到数据库，主要是通过云服务器进行采集及保存，业主智能体将定期从这个数据库获取相关的数据。
主要的方式：
方式1：根据设定关键字，按照每日设定频率，自动获取相关SKU的前2个内容的价格。

你在开发的时候采用最简单的方式去做，比如辅助功能，或者你根据你的理解去做，只要结果准确就可以；
你可以先跑通一个app比如美团快驴、其他的app依次复用跑通，直至全部可以；跑通一个app后先存git存入，防止后续乱了也可以回退；

完成需求，不需要太复杂，怎么简单怎么来，可以只有一个悬浮窗悬浮在其他app上，点击采集自动采集相关SKU的价格信息，将采集的数据显示出来，然后后续有接口了，将采集到的数据通过接口上传到服务器。
请你分步完成，每个步骤完成后，都要测试一下，确保功能正常。
1. 先跑通一个app比如食行生鲜，将相关SKU的价格信息采集出来，显示出来悬浮窗上。

